<div class="citiesList">
  <% @city.each do |t|%>
  <div class="city">
    <%= image_tag (t.photo), class: "cityPic"%>
    <p class="name"><%=link_to "#{t.name}", city_path(t)%></br><small><%= t.lat%><%= t.lon%></small></p>

  </div>
  <%end%>
</div>


<div id="map"></div>
<script>
  var map;
  function initMap() {
    var myLatLng = {lat: -25.363, lng: 131.044};

    // Create a map object and specify the DOM element for display.
    var map = new google.maps.Map(document.getElementById('map'), {
      center: myLatLng,
      scrollwheel: false,
      zoom: 4
    });
}
    // Create a marker and set its position.
    // <%# @city.each do |city| %>
    // // var marker = new google.maps.Marker({
    // //   map: map,
    // //   position: {lat: city.lat}, lng: #{city.lon}},
    // //   title: #{city.name}
    // // });
    // <%# end %>
  // }
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAN1LxqNsMp8I6WYGLxd_ElhURRVfGaNO0&callback=initMap"
async defer></script>
