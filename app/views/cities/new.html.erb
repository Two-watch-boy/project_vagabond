Add a city

<div class="container">
  <%= form_for @city do |f| %>
  <div>
    <%= f.text_field :name, {placeholder: "City Name", class: "form-control"} %>
  </div>
  <%= f.submit class: "btn btn-success btn-block"%>
  <% end %>
